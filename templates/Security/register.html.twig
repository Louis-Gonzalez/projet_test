{% extends "base.html.twig" %}

{% block title %}
    | S'inscrire
    {{ parent()}}
{% endblock %}

{% block body %}
    <h1>S'inscrire</h1>
    {{form_start(form)}}
    {{form_widget(form)}}
        <button type="submit">S'inscrire</button>
    {{form_end(form)}}
{% endblock %}